# Copy this to .env for local dev (vercel dev picks it up automatically).
# In production, set this as an environment variable in your Vercel project settings instead.
GEMINI_API_KEY=your_gemini_api_key_here
